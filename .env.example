# Database
DATABASE_URL="mongodb+srv://username:password@cluster.mongodb.net/database"

# Auth.js v5
AUTH_SECRET="your-secret-key-here-generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"

# API Configuration
# Direct backend API URL (CORS is fixed on backend, so we use direct communication)
NEXT_PUBLIC_API_URL="https://pishro-0.vercel.app/api"
# Backend URL (kept for backward compatibility)
BACKEND_API_URL="https://pishro-0.vercel.app/api"
